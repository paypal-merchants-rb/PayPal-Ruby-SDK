!!!
%html
  %head
    %title PayPal Rest API Samples
  %body
    %a{:href => ".."} Back
    - if display_hash.success?
      %h3= header
      .display-hash
        = CodeRay.scan(MultiJson.dump(display_hash.to_hash, :pretty => true), "json").div
    - else
      %h3 Error while #{header}
      .display-hash
        = CodeRay.scan(MultiJson.dump(display_hash.error, :pretty => true), "json").div
    %a{:href => ".."} Back    
        
